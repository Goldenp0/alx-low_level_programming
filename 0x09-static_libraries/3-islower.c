#include "main.h"
/**
  * _islower - program checks for lowercase letters
  * @c: the letters to check
  * Return: 1 if c is lowercase, 0 if not so
  */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
